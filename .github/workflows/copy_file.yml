name: Upload File

on:
  push:
    branches:
      - main

jobs:
  upload-file:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Upload file
        run: |
          mkdir -p Test
          cp C:\D_drive\GCP\SSH\Powershell_ssh.txt Test/
          git add Test/file
          git commit -m "Add file"
          git push
